- route leaks: показывать время сушествования каждого роут-лика:
  - апдейтить при сохранении в mysql, чтобы было видно время появления/пропадания и за пределами заявленного интервала;
  - сохранять crc32(aspath), причём aspath без взаимоотношений, и искать лик по этому crc32
- change bsearch to hash in mkrel()
- считать устойчивость (надёжность, дисперсия показателей по времени);
- считать связность каким-то образом;
- исключить совпадающие as-path от разных пиров (с разными orig-as)
- средняя длина fullview для каждого ISP (?)
